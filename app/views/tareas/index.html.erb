<h1>Mis tareas</h1>

<table class="table table-hover table-striped">
	<thead>
		<th>Tarea</th>
		<th>Editar</th>
		<th>Eliminar</th>
	</thead>
	<tbody>
	<% @tareas.each do |tarea| %>
		<tr>
			<td><%= link_to tarea.titulo,tarea %></td>
			<td><%= link_to "Editar", edit_tarea_path(tarea), class: "btn btn-warning"%></td>
			<td>
				<%= link_to "Eliminar", tarea, method: "delete", class: "btn btn-danger", data: {confirm: "¿Quieres eliminar esta tarea?"} %>
			</td>						
		</tr>
	<% end %>
	</tbody>
</table>